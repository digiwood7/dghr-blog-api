# dghr-blog-api (ARCHIVED)

> ⚠️ **이 저장소는 archive 되었습니다 (2026-04-30).**
> 활성 저장소: **[`digiwood7/dghr`](https://github.com/digiwood7/dghr)** 의 `backend/` 디렉토리.

## 이력

이 저장소는 DigiWood Blog/Honeycomb ERP 백엔드(FastAPI)의 **옛 미러**입니다.
실제 운영(Railway 자동 배포 `web-production-559e3.up.railway.app`)은
`digiwood7/dghr` 모노레포의 `master` 브랜치, `backend/` 디렉토리에서 진행됩니다.

## 마이그레이션 안내

- 새 코드는 [`digiwood7/dghr`](https://github.com/digiwood7/dghr) 의 `backend/`에 push 하세요.
- Railway 서비스(`dghr-blog`)는 `dghr/master` 의 GitHub auto-deploy 로 동작합니다.
- 본 저장소에 신규 push 해도 운영 환경에 반영되지 않습니다.

## 저장소 정리 시점

2026-04-30 — 백엔드 운영 위치를 `digiwood7/dghr/backend/`로 일원화.
